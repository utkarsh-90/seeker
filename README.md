# Seeker

Multi-model RAG platform: FastAPI API and Next.js web app.

## Structure

```
apps/
├── api/    # FastAPI service (Python, uv)
└── web/    # Next.js 15 frontend
```

## Dev

Clone the repo, run `pnpm install`, then `docker compose up` for local data services.
